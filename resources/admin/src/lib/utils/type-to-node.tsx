import { FC, ReactNode } from "react";
import { IconType } from "react-icons/lib";

// interface IconRendererProps {
//     icon: IconType;
// }
// export const iconToNode = (props: IconRendererProps) => {
//     const {icon: Icon} = props;
//     return <Icon size={18}></Icon>
// };