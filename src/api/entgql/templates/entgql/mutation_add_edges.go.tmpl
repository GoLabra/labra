// Code generated by internal template, DO NOT EDIT.

package ent

import (
	"fmt"
	"time"
	{{- range $n := $.Nodes}}
	"app/ent/{{$n.Package}}"
	{{- end}}
)

{{- range $n := $.Nodes }}
// AddEdges sets the value of a field with the given name. It returns an error if
// the edge is not defined in the schema
func (m *{{$n.Name}}Mutation) AddEdges(name string, values []string) error {
	switch name {
	{{- range $e := $n.Edges }}
	{{- if not $e.Unique}}
	case {{$n.Package}}.Edge{{$e.StructField}}:
	    m.{{$e.MutationAdd}}(values...)
	    return nil
	{{- end}}
	{{- end}}
	}
	return fmt.Errorf("unknown {{$n.Name}} edge %s", name)
}
{{- end}}