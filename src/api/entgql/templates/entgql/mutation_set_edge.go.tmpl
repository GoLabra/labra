// Code generated by internal template, DO NOT EDIT.

package ent

import (
	"fmt"
	"time"
	{{- range $n := $.Nodes}}
	"app/ent/{{$n.Package}}"
	{{- end}}
)

{{- range $n := $.Nodes }}
// SetEdge sets the value of a field with the given name. It returns an error if
// the edge is not defined in the schema
func (m *{{$n.Name}}Mutation) SetEdge(name, value string) error {
	switch name {
	{{- range $e := $n.Edges }}
	{{- if $e.Unique}}
	case {{$n.Package}}.Edge{{$e.StructField}}:
	    m.{{$e.MutationSet}}(value)
	    return nil
	{{- end}}
	{{- end}}
	}
	return fmt.Errorf("unknown {{$n.Name}} edge %s", name)
}
{{- end}}